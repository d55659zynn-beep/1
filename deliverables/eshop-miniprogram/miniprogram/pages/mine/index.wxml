<view class="page">
  <view class="header">
    <view class="header__avatar"></view>
    <view class="header__info">
      <view class="header__name">{{userInfo.nickName || '微信用户'}}</view>
      <view class="header__id">ID 尾号 {{openidTail}}</view>
    </view>
  </view>

  <view class="card menu">
    <view class="menu__item" bindtap="goOrders">
      <text>我的订单</text>
      <text class="menu__arrow">›</text>
    </view>
    <view class="menu__item" bindtap="goAdmin">
      <text>商品管理（上架 / 下架）</text>
      <text class="menu__arrow">›</text>
    </view>
    <view class="menu__item" bindtap="goOrderManage">
      <text>订单管理（发货通知）</text>
      <text class="menu__arrow">›</text>
    </view>
    <view class="menu__item menu__item--plain">
      <text>客服与售后</text>
      <text class="muted">工作日 9:00 - 18:00</text>
    </view>
  </view>

  <view class="tip" wx:if="{{!isAdmin}}">
    提示：把 users 集合里自己那条记录的 isAdmin 改成 true，即可解锁「商品管理」。
  </view>
</view>
